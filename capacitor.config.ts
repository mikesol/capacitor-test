import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fm.joyride.bctest',
  appName: 'butcrusher',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
